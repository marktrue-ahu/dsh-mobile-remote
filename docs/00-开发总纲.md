# 00 开发总纲 — DSH Mobile（手机远程操作端）
> 版本：v2.7.0 · 状态：已开源发布，持续维护 · 本文件是后续全部工作的最高依据
## 1. 项目一句话

**把 dsh（DeepSeek Harness）变成随身控制台**：手机随时查看 agent 状态、发消息派活、审批决策、接收完成/待回答/失败通知、管理会话——电脑端跑着，人不在电脑前也能全程操作。
## 2. 资产现状（开发起点）

| 资产 | 状态 | 位置 |
|---|---|---|
| `dsh-mobile-remote` host 插件（/m/api + 桌面客户端模块） | ✅ 已安装启用（desktop profile；web profile 亦可） | `<插件安装目录>` |
| 桌面端兼容 | ✅ 已验证（DSH Desktop v2.0.2 / 内核 0.1.1-rc.2，LAN 桥接入） | deepseek-harness-desktop（本机验证） |
| 移动端界面原型 v7（DeepSeek 配色） | ✅ **已定稿** | `prototype\app-prototype.html` |
| 既有文档 01~09 | ✅ 已更新到 v3.0.0 产品版 | `docs\` |
| 发布产物 | ✅ 版本线统一：App 版本 = 插件版本 = git tag；Release 每版双附件（APK + 插件 tgz） | GitHub Releases |

## 3. 设计定稿（界面冻结，开发必须遵守）

- **视觉**：DeepSeek 品牌配色（主色 `#426EFE`，深色 `#0E1116`）、衬线大标题、1.5 线宽线性图标、大圆角、默认跟随系统深浅色
- **信息架构**：侧边栏三项（首页 / 会话 / 设置）；对话、通知为详情页；无底部 tab
- **首页固定欢迎界面**（不做状态切换）：欢迎语 + 最近会话 + 新建会话 + 输入权限
- **新建会话当场选模式**（标准 PTC/极简/创造，预设 · 设置默认值）
- **所有展示数据真实对齐 PC 端**：模型（deepseek-v4-flash / deepseek-v4-pro）、推理强度（off/high/max）、权限（read-only / workspace-write / danger-full-access，Full Access 需风险确认）、模式显示名（标准 PTC/极简/创造）
- **诚实原则**：未实现的功能明确标注「开发中」，不出现无声摆设；深色模式默认跟随系统
- **移动端不感知插件**：核心层只依赖 dsh 内核语义；动作区（可选）打开时实时拉取清单；插件管理永远在 PC 端
## 4. 开发阶段（依赖顺序）
> 现状（v2.7.0）：Phase 1-4 全部完成；网页版已移除（见 Phase 3 注）；v2.2~v2.5 补充：消息操作/通知删除/问询审批弹窗/无限上翻/多地址切换/连接自愈/版本线统一等（详见 Phase 4 与 CHANGELOG）；**v2.6.0 安全加固 + 过程可见性 + 模型提供商互通**：登录限流、推送脱敏、链接白名单、qr.png 收口、口令加固、认证警示、备份隔离、活动条/思考面板、37 个 dormant 提供商手机可见可配、llm-providers 端点；**v2.7.0 会话工具 + 体验打磨**：任务进度实时推送、子代理/目标手机端可见可操作（jobs/subagents/goal 端点）、agent 状态 bootstrap 同步、下拉刷新收集地址、聊天草稿保留、首页改版（官方 logo / 新欢迎语 / 最近会话卡片滚动）。
### Phase 1：后端 API 补全（完成 ✅）

**范围**：插件新增移动端所需的全部 API。
| 交付版 | 验收标准 |
|---|---|
| 模型目录 API（读取） | 返回 PC 端真实模型 + 推理强度选项与当前值 |
| 会话配置 API（读写） | 切换当前会话模型/推理强度/权限预设；风险确认后允许 danger-full-access |
| Agent 预设 API（读写） | 读默认预设；新建会话带预设参数 |
| 新建会话 API | 按 preset 创建会话，返回 id 可续接 |
| 通知中心 API | 聚合完成/待回答/失败事件；未读 已读状态 |
| 动作区契约 v0.1 | GET 动作清单 + POST 执行（注册服务 `ctx.mobileActions`） |

**Phase 1 完成标志**：API 全链路可用（e2e-check 通过）。
### Phase 2：推送桥（完成 ✅）

**范围**：agent 完成 / 需要回答 / 失败三类事件 → 推送服务（Bark / Serverchan / ntfy，可配置）→ 手机系统通知；App 在后台也能收到。
| 交付版 | 验收标准 |
|---|---|
| 推送桥配置（profile settings） | 配置推送服务 URL + 开关 + 事件类型选择 |
| 事件 → 推送转发 | 三类事件触发推送，内容含会话摘要；同会话同类型聚合 |

**Phase 2 完成标志**：手机锁屏收到任务完成/需要回答通知。
### Phase 3：Flutter App（完成 ✅）

**范围**：按定稿原型实现原生 App，对接 Phase 1 API。
| 交付版 | 验收标准 |
|---|---|
| Flutter 工程（Android 优先） | 全部界面与弹窗按原型实现 |
| 连接管理 | 扫码/手动连接、重连、断线提示 |
| 通知接入 | 前台 SSE + 后台依赖 Phase 2 推送桥 |
| 打包 | Android APK 可安装 |

**Phase 3 完成标志**：APK 安装后全功能可用，体验等于原型。
> **v2.1 决策**：网页版（`/m` 页面）在 App 完成后移除（不再提供浏览器形态），减少运行资源；桌面设置页由客户端模块（`client.js`）提供扫码入口。
### Phase 4：移动端补齐与可靠性加固（完成 ✅）

**范围**：把「人不在电脑前」做实——人类交互补全（问询/审批）、连接不丢、断了快恢复、版本不混乱。
| 交付版 | 验收标准 |
|---|---|
| 问询/审批弹窗（v2.4.0） | agent 提问/工具越权时手机弹卡片，与 PC 同一 pending 通道，任一端回答两端同步消失 |
| 多地址自动切换（v2.4.1） | 连接成功后自动收集全部地址（局域网 IP + 虚拟组网 IP），断线重试失败自动轮换——出门切虚拟组网、回家切局域网，全程免手动 |
| 连接自愈（v2.4.2/v2.5.1） | SSE 心跳看门狗（75s 无心跳强制重建）；黑洞地址故障切换 72s→约 10s；下拉刷新「探测→自愈→拉数据」 |
| 手动切换连接地址（v2.5.0） | 设置 → 电脑地址点按弹出候选列表，探测可达后切换并重连 |
| 版本线统一（v2.5.1） | App 版本 = 插件版本 = git tag；Release 每版双附件（APK + 插件 tgz） |
| 布局溢出硬化（v2.5.2） | 工作区抽屉/底部弹层/问询卡片/连接配置页在内容多、小屏 + 键盘场景不溢出 |

**Phase 4 完成标志**：手机断网/组网失效后约 10 秒自动切到可用地址；抽屉与弹层在极端内容量下不破版。

## 5. 阶段依赖与顺序理顺
1. **API 先行**：App 与脚本测试共用同一套 API，先写 API 再写客户端
2. **推送桥在 App 前**：App 的后台通知直接复用推送桥，不必在 Flutter 里重做
3. **App 最后**：纯锦上添花，风险最低的部分最后做

## 6. 关键决策记录（ADR 摘要）
| # | 决策 | 理由 |
|---|---|---|
| D1 | 视觉 = DeepSeek 品牌色 + Claude/Notion 排版 | 用户定稿；暖色、蓝色都试过，DeepSeek 蓝最对味 |
| D2 | 首页固定欢迎界面，无状态切换 | 任务断断续续时切换体验差（用户明确要求） |
| D3 | 新建会话当场选模式 | 避免每次去设置改默认（用户明确要求） |
| D4 | 权限三档 + Full Access 风险确认 | 与 PC 端沙箱模式一致 |
| D5 | 移动端零插件感知 + 可选动作区 | 插件生态差异不进入移动端核心层 |
| D6 | 诚实原则（开发中标注） | 避免虚假功能误导 |
| D7 | 桌面端 命令行双形态兼容 | 同 profile 同 CLI，插件天然兼容 |
| D8 | 版本线统一：App 版本 = 插件版本 = git tag，Release 双附件 | v2.5.1；避免版本错配困惑；已发布接口只加不删（破坏性变更禁止） |
| D9 | 连接自愈多级兜底：SSE 超时 + 心跳看门狗 + 地址轮换 | 断连形态多样（静默死亡/黑洞/半开），单一机制覆盖不全 |
| D10 | 地址策略：自动切换为主、手动切换兜底 | 自动覆盖日常（出门/回家）；特殊组网场景留给用户手动选 |
| D11 | 登录失败限流（10 次/60s/来源 IP，成功重置） | 防弱口令爆破；正常用户零感知（v2.6.0） |
| D12 | 推送内容默认脱敏（minimal：仅事件类型+会话短码） | 第三方推送通道（Server酱/ntfy/Bark）不可信；核心内容默认不出本机（v2.6.0） |
| D13 | 消息链接 scheme 白名单（仅 http/https 可点击） | 防 file:/intent:/tel: 拉起任意应用（v2.6.0，App） |
| D14 | qr.png 与 qr-config 同策略（Host 校验 + loopback only） | 消除唯一无认证路由；桌面设置页本就要求 loopback 拉数据（v2.6.0） |

## 7. 风险与应对
| 风险 | 应对 |
|---|---|
| 会话配置 API 需要权限边界（改模型 权限影响会话行为） | 写操作要求认证 + 与 PC 端相同的风险确认流程 |
| 通知聚合的事件语义（待回答如何判定） | 已实现：`turn/end` reason.kind=blocked → needs-answer（v2.4 与问询/审批弹窗联动） |
| 动作区契约被第三方插件滥用 | 注册表冲突抛错（Cordis 语义） 文档约定命名空间 |
| 问询/审批桥依赖 apiProxy 私有协议 | 干净降级（不崩、不弹窗），诊断可查，随插件版本跟进——见 09-compatibility |
| 组网/隧道断连产生黑洞地址（连接不拒绝但无响应） | SSE 超时 + 心跳看门狗 + 地址轮换多级自愈（v2.5.1 实测 72s→约 10s 恢复） |
| 国产 ROM 杀后台导致外出断连/漏通知 | 文档引导保活设置（docs/06 蒲公英手机端），App 侧地址自愈兜底 |
| 弱口令被爆破（早期无速率限制） | v2.6 登录限流：10 次/60s → 429 + Retry-After；认证成功重置计数 |
| 推送内容经第三方通道泄露（会话标题/错误详情） | v2.6 推送默认脱敏：只推事件类型 + 会话短码（pushContent: minimal） |

## 8. 文档索引

- 本文件：总纲（最高依据）
- `01-PRD.md`：产品需求（v0.1/v1 历史 + v2.0 移动端产品版）
- `02-architecture.md`：架构（插件 + App）
- `03-api.md`：API 设计（全部端点 + SSE 帧）
- `04-security.md`：安全设计
- `05-test-cases.md`：测试用例（含 v2.3 问询/审批、通知删除端到端用例；连接逻辑另有单元测试，见 §4 回归建议）
- `06-install-run.md`：部署与启用（安装/配置/构建/签名）
- `07-user-manual.md`：用户手册（含 FAQ）
- `08-扩展开发指南.md`：二次开发（动作区/推送/App 定制/分享你的版本）
- `09-compatibility.md`：兼容性（内核耦合点/多品牌/已知问题）
