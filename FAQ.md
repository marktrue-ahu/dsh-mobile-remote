# FAQ（常见问题）

## 连接类

**Q：手机连不上（连接失败 / 超时）？**
1. 确认电脑端 DSH 已启动；**桌面版（0.1.1-rc.2）webserver 强制只听 127.0.0.1，手机必须走插件 LAN 桥**（`lanBridge.enabled: true`，默认 `0.0.0.0:3080`，见 docs/06 §4b）；web 版才用 `webserver: {host: 0.0.0.0}`（且覆盖 webserver 行必须写全 `port` 必填字段，见 docs/06 §4 警示）
2. 手机和电脑必须在同一网络（同一 WiFi / 虚拟组网）
3. 口令是否正确（`cordis.patch.yml` 的 `authToken`）；改了口令要重启桌面端
4. **Windows 防火墙（最常见）**：路由器重启/换网后 Windows 会把网络重新识别为「公用」，默认拦截入站 3080。修复：
   - 设置 → 网络和 Internet → 当前网络 → 网络配置文件类型改为「**专用网络**」；
   - 或以管理员身份运行 PowerShell：
     ```powershell
     netsh advfirewall firewall add rule name="DSH Mobile 3080" dir=in action=allow protocol=TCP localport=3080 profile=private
     ```
   - 判断方法：电脑上 `http://127.0.0.1:3080/m/api/bootstrap` 能通、手机却连不上，基本就是防火墙/网络类别问题

**Q：扫码连不上 / 二维码扫了没反应？**
- App 请用自带的「扫码连接」按钮，不要用系统相机（系统相机会跳浏览器打开网页）
- 二维码含口令，改了 `authToken` 后需重启桌面端，二维码自动更新
- 手机与电脑需同一网络

**Q：外出（不在家）怎么连？**
**推荐：蒲公英组网**（已实测 ✅）——电脑和手机装蒲公英客户端，登录同一账号加入同一组网（免费版 3 个成员）。手机 DSH App 首次手动填一次电脑的蒲公英虚拟 IP（`http://172.16.x.x:3080`），之后 App 自动切换：出门走蒲公英、回家走局域网（设置 → 电脑地址显示「共 N 个地址自动切换」）。步骤详见 docs/06 §5.1。
其他虚拟组网（ZeroTier/路由器 WireGuard）与内网穿透（frp/樱花映射）同理可接，配置要点见 docs/06 §5.2（隧道类需插件 `trustedHosts` 放行 + 强口令）。**禁止公网端口裸奔映射**；Tailscale 因国内控制面不可达不推荐。

## 使用类

**Q：发消息没反应？**
- agent 正在处理上一轮时新消息会**排队**（dsh 机制），移动端会提示；等上一轮完成即可
- 检查状态点：绿色=运行中，橙色=等待你回答（直接回复即可）

**Q：通知不推送？**
- 检查 `pushUrls` 配置是否正确 + 重启桌面端
- Server酱：SendKey 是否有效；ntfy：手机 App 是否订阅了同一 topic
- 同会话同类型通知 60 秒内合并（`pushCooldownMs`）

**Q：通知列表里看不到任务完成？**
通知中心按会话聚合（同会话同类型只保留一条）；PC 端任务完成/失败/需回答才会产生通知。

**Q：新建会话选的工作目录在 PC 端显示"未分组"？**
该目录不在任何已注册工作区（或不在其子目录下）。在工作区子目录下会自动归属；也可在 PC 端把该目录注册为工作区。

**Q：会话列表和 PC 端不一致？**
不会 —— App/网页端与 PC 端共用同一会话数据源（实时读写），标题/分组/归档全部同源。

## 问询/审批弹窗类

**Q：agent 问我问题/要权限时，手机为什么不弹卡片？**
1. 弹窗在**该会话的对话页**里；人在别的页面时只有通知中心的「需要你回答」条目，点进去即见弹窗
2. 电脑端 dsh 需重启过（弹窗桥随插件加载）；App 设置 → 环境诊断确认 `respondBridge`/`frameBridge` 为 ✅
3. 若 PC 端也没弹：会话审批策略为「从不询问」，或 Harness 配了自动答题/自动审批（不弹是正常行为）
详见 docs/09-compatibility.md。

**Q：手机点了 ✕ / 回答了，电脑端会怎样？**
两端同一待办：手机 ✕ = 取消（agent 按取消继续），手机回答 = 答案直接传给 agent；任何一端处理后另一端弹窗同步消失。先到先得，后答的一方提示"可能电脑端已先处理"。

## 设备/兼容类

**Q：哪些手机能用？有苹果版吗？**
Android 7.0+ 各品牌均可（Flutter 原生，渲染自动回退）；实测机型：小米 17 Pro Max。**苹果端尚未开发**——开发者没有苹果设备（Mac/iPhone），无法构建与真机调试；代码本身已平台无关，欢迎有 Mac 的开发者贡献 iOS 版本（见 docs/09-compatibility.md §4）。

**Q：换手机 / 重装 App 后要重新连接吗？**
要：连接信息只存手机本地。重新扫一次电脑端二维码即可（口令未变则电脑端不用动）。

**Q：为什么我装的 App 升级时提示签名不一致？**
构建者更换了签名 keystore（签名变更 = 新应用）。卸载旧版重装，然后重新扫码。开发/自用构建回退 debug 签名，公开分发必须用同一正式 keystore（见 dsh-mobile-app/README.md）。

## 深色/显示类

**Q：界面是黑的 / 想换浅色？**
设置 → 深色模式：跟随系统 / 深色 / 浅色 三态切换（App）；网页端跟随浏览器系统主题。

**Q：看不到工具调用过程？**
对话页输入框上方有**活动条**：思考中 / 正在调用工具（如 read）实时显示，结束后消失；思考内容可点开查看（v2.6 起）。工具结果细节以 PC 端为准。

## 安全类

**Q：二维码/口令泄露了怎么办？**
修改 `cordis.patch.yml` 的 `authToken` → 重启桌面端 → 二维码自动更新，旧码作废。

**Q：可以把 3080 映射到公网吗？**
**禁止**。插件面向可信内网设计（详见 docs/04-security.md）；外出请用**蒲公英组网**（推荐，docs/06 §5.1；Tailscale 国内控制面不可达不推荐，其他组网/隧道思路见 §5.2，公网隧道必须配 TLS）。

**Q：App 会联网上传我的数据吗？**
不会。App 只与本机配置的电脑地址通信（HTTP 直连），连接信息仅存手机本地。
