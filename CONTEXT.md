# DSH 移动控制

本项目让手机用户访问 DeepSeek Harness 中的会话、运行状态以及需要用户参与的操作。

## Language

**宿主**：运行 DeepSeek Harness 并承载会话与 Agent 的实例，可以采用桌面或 Web 形态。

**移动控制面**：手机用户可使用的会话、消息、模型、队列、通知、问询、审批及相关管理功能的集合。

**双版本兼容**：同一插件版本同时支持 DSH 0.1.1-rc.2 和 0.1.2-rc.1，保持现有 App 的操作语义。

**完整兼容验收**：移动控制面的现有功能在约定环境中逐项通过验证；因宿主缺陷而降级的功能仍属于验收阻塞。

**问询**：Agent 请求用户提供结构化回答的交互。
_Avoid_: 审批（回答问题与允许执行是不同的用户决策）。

**审批**：用户对 Agent 请求执行某项操作作出允许或拒绝决定的交互。
_Avoid_: 问询（审批具有明确的授权含义）。
