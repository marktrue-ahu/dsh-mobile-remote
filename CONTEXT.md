# DSH Mobile Git 管理上下文

本上下文定义手机端 Git 管理使用的领域语言。Git 事实和操作属于电脑端工作区，手机端只呈现事实并表达用户意图。

## 仓库

**Git 工作区**：
经 DSH 工作区授权、包含一个 Git 仓库的电脑端目录，是 Git 操作的安全边界。
_避免_：当前项目、手机目录、任意 cwd

**仓库上下文**：
Git 管理界面当前指向的 Git 工作区；会话只能提供默认定位线索，不能替代明确的仓库选择。
_避免_：当前会话、客户端路径

**仓库标识**：
由电脑端为仓库上下文分配的稳定身份，手机端用它引用仓库而不把主机路径当作操作权限。
_避免_：cwd、绝对路径

**当前分支**：
仓库上下文中当前检出的本地分支；仓库处于 detached HEAD 时不存在当前分支。
_避免_：远端分支、会话分支、HEAD

**同步目标**：
一次 pull 或 push 明确选择的远端与目标分支，通常来自当前分支的 upstream。
_避免_：默认远端、隐式 origin

## 移动交互

**Git 快捷栏**：
消息发送框下方的三个可配置 Git 功能入口，默认显示当前分支、分支图和远端同步。
_避免_：动作区、插件栏

**功能槽位**：
Git 快捷栏中可配置和排序的位置；一个功能至多占用一个槽位。
_避免_：按钮列表

**分支抽屉**：
用于浏览本地与远端分支并发起分支操作的移动面板。
_避免_：分支图、提交列表

**分支图**：
按提交拓扑展示分支、远端引用、标签和提交历史的只读导航视图。
_避免_：分支抽屉、线性提交列表

**分支图选择集**：
一次分支图查询明确选择的本地或远程引用集合；引用名称表达身份，查询建立时的 tip OID 固定其历史起点。
_避免_：当前可见颜色、所有分支、提交所属分支

**分支图快照**：
把仓库、分支图选择集和排序边界绑定在一起的短期只读查询身份，用于保证多页提交属于同一次拓扑观察。
_避免_：完整提交缓存、实时分支状态、App 本地快照

**显示 lane**：
分支图为表达父子、分叉和合并关系而计算的视觉轨道；它不是 Git 分支，也不表示提交归属于某个分支。
_避免_：分支所有权、选中分支数量、提交分支

## Git 操作

**日常 Git 闭环**：
手机端优先支持的短时高频工作流，包括查看状态和差异、暂存、提交、分支操作以及与远端协作。
_避免_：完整桌面 Git 工作台

**远端同步**：
围绕一个明确同步目标执行 fetch、pull 和 push 的分阶段协作流程，任一步失败都会停止后续步骤。
_避免_：单纯抓取、原子同步

**受保护切换**：
切换分支前保护可能被覆盖的工作区改动，并在需要处理时要求用户明确选择后续动作。
_避免_：强制切换、所有脏工作区一律禁止切换

**破坏性 Git 操作**：
可能丢失提交、引用或工作区内容的操作，需要独立于普通认证的明确确认。
_避免_：普通写操作、客户端弹窗确认

**Git 操作任务**：
由电脑端拥有、可在手机断线后继续查询结果的 Git 操作实例；同一仓库的写任务按顺序执行。
_避免_：长 HTTP 请求、客户端任务

**操作取消**：
请求尽力终止仍在运行的 Git 进程，不承诺撤销已经产生的仓库或远端变化。
_避免_：回滚、Git abort

**冲突交接**：
Git 操作因冲突暂停后，由用户选择终止相关 Git 流程、交给模型处理或转到电脑继续。
_避免_：自动解决、普通任务取消

**提交消息建议**：
模型基于用户允许的暂存内容生成、且必须由用户确认的可编辑提交消息候选。
_避免_：自动提交、不可编辑消息、静默上传 diff

## 能力边界

**Git 能力接缝**：
连接 Git 服务定义、一个或多个能力提供者以及能力消费者的稳定边界。
_避免_：某个插件的私有路由、移动端 Git 实现

**Git 服务定义**：
独立描述 Git 状态、操作、任务、事件和错误语义的 DSH 服务契约，不依赖具体提供者或移动协议。
_避免_：TypeScript 接口、provider DTO、HTTP API

**Git 能力提供者**：
在 DSH 进程内实现 Git 服务定义并拥有仓库事实和操作生命周期的组件。
_避免_：Flutter App、移动适配器、私有 HTTP 路由

**Git 移动适配器**：
把 Git 服务定义投影为稳定移动契约的 mobile-remote 组件，拥有移动鉴权、兼容降级和确认边界，但不拥有 Git 状态。
_避免_：Git provider、终端文本解析器

**Git 能力可用性**：
移动适配器根据服务定义和兼容版本报告的功能集合；不可用能力必须给出明确原因。
_避免_：插件名称、静默隐藏、固定功能白名单
